/** Distinct videos with a passing comprehension score required to advance one phase. */
export const DISTINCT_PASSED_LESSONS_PER_PHASE_STEP = 2;

/** Minimum score on the phase final test (MCQ) to unlock the next phase. */
export const PHASE_FINAL_TEST_MIN_SCORE_PCT = 70;

/** Number of MCQ items in a phase final test. */
export const PHASE_FINAL_TEST_QUESTION_COUNT = 10;
