/** Minimum quiz score (%) to count a watched video toward leaderboard activity. */
export const LEADERBOARD_MIN_SCORE_PCT = 80;

/** Maximum rows returned on the public leaderboard. */
export const LEADERBOARD_TOP_LIMIT = 50;
